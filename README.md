
  # PhxNorth (new version) (Copy) (Copy)

  This is a code bundle for PhxNorth (new version) (Copy) (Copy). The original project is available at https://www.figma.com/design/KG7LbQRnDLng08HpwlOFvt/PhxNorth--new-version---Copy---Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  